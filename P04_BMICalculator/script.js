const heightValue = document.getElementById("height");
const weightValue = document.getElementById("weight");
const generateBtn = document.querySelector("button");

generateBtn.addEventListener("onclick", () => {});
